Navisu is a nautical software which is allow you to display chart with WorldWind.

Before starting NaVisu, go into the worldwind cache directory.

On linux : ~/var/WorldWindData/Earth
On Windows : C:/ProgramData/WorldWindData/Earth

Create a folder called KapLayer and put your tiles inside it.

Now, you can launch NaVisu and create the database by selecting the directory which contains your .kap files and here we go.
